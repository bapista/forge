# Copy to inventory.ini and fill in your own hosts (inventory.ini is gitignored).
# Sanitised example — replace with your real (private) addresses.
[server]
the-core ansible_host=10.0.0.10 ansible_user=admin

[agents]
pawn     ansible_host=10.0.0.21 ansible_user=admin   node_role=edge-npu
envoy    ansible_host=10.0.0.22 ansible_user=admin   node_role=mail
cavalry  ansible_host=10.0.0.23 ansible_user=admin   node_role=compute
templar  ansible_host=10.0.0.24 ansible_user=admin   node_role=gateway

[k3s_cluster:children]
server
agents
