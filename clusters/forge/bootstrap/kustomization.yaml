apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Argo CD install (pinned upstream + namespace). app-of-apps takes over after this.
namespace: argocd
resources:
  - namespace.yaml
  - https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
